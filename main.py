#Dont touch
import engine.engine as eng

#eng.start() starts the game engine
#eng.wait(seconds) waits for the specified amout of seconds
#eng.clearscreen() clears the console
#eng.colorpixel(x, y, color) colors the specified pixel
#eng.newpixelsprite(x, y, color, name) creates a new sprit that can be referenced later
#eng.changepscolor(name, color) changes the specified sprites color
#eng.moveps(name, newx, newy, offsetx, offsety) moves the position to the newx and newy values unless they are both -1 then it will offset by the offsetx and offsety
#eng.clearpixelsprites() clears all sprites
#eng.refresh() prints the game board